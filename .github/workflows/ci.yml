name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  tests:
    name: Julia Tests (${{ matrix.julia-version }} on ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        julia-version: ['1.10', '1.11']

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Julia
        uses: julia-actions/setup-julia@v1
        with:
          version: ${{ matrix.julia-version }}

      - name: Cache Julia artifacts
        uses: julia-actions/cache@v1

      - name: Instantiate
        run: julia --project -e 'using Pkg; Pkg.instantiate(); Pkg.precompile()'

      - name: Run tests
        env:
          JULIA_NUM_THREADS: '2'
          JULIA_DEBUG: FeastKit
          # Without these the high-level feast() smoke run and the whole
          # parallel-support block were skipped on every CI run.
          FEAST_RUN_LONG_TESTS: 'true'
          FEAST_RUN_PARALLEL_TESTS: 'true'
          FEASTKIT_TEST_PARALLEL: 'true'
        run: julia --project -e 'using Pkg; Pkg.test()'

  distributed-backend:
    name: Distributed Backend
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Julia
        uses: julia-actions/setup-julia@v1
        with:
          version: '1.10'

      - name: Cache Julia artifacts
        uses: julia-actions/cache@v1

      - name: Instantiate
        run: julia --project -e 'using Pkg; Pkg.instantiate(); Pkg.precompile()'

      - name: Run distributed backend tests
        env:
          FEASTKIT_TEST_DISTRIBUTED: 'true'
        run: |
          julia --project -e '
            using Distributed
            addprocs(2; exeflags=`--project=$(Base.active_project())`)
            @everywhere using FeastKit, LinearAlgebra, SparseArrays
            include("test/test_parallel_backends.jl")
          '

  mpi-backend:
    name: MPI Backend
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Julia
        uses: julia-actions/setup-julia@v1
        with:
          version: '1.10'

      - name: Cache Julia artifacts
        uses: julia-actions/cache@v1

      - name: Instantiate
        run: julia --project -e 'using Pkg; Pkg.instantiate(); Pkg.precompile()'

      - name: Run MPI backend tests
        env:
          FEASTKIT_ENABLE_MPI: 'true'
          FEASTKIT_TEST_MPI: 'true'
        run: |
          julia -e '
            using Pkg
            # MPI is a weak dependency, so it is not in the package project.
            # Build a throwaway env that has both, which is also what activates
            # the FeastKitMPIExt extension.
            env = mktempdir()
            Pkg.activate(env)
            Pkg.develop(path=".")
            Pkg.add(["MPI", "Krylov"])
            using MPI
            run(`$(MPI.mpiexec()) -n 2 julia --project=$(env) test/test_parallel_backends.jl`)
          '

  docs-links:
    name: Docs Link Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Link Checker
        uses: lycheeverse/lychee-action@v1.9.0
        with:
          args: >-
            --verbose --no-progress --max-concurrency 8
            --exclude-mail
            --accept 200,206,429
            docs/**/*.md
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
